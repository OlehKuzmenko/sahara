import NotificationBar from '@/components/NotificationBar';
import React from 'react';

export default function Farming(): JSX.Element {
  return (
    <div>
      <NotificationBar
        text='In the short run, you will be able to invest in pools, lend and borrow crypto coins'
      />
    </div>
  );
}
