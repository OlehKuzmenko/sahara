import NotificationBar from '@/components/NotificationBar';
import React from 'react';

export default function PrivateSwap(): JSX.Element {
  return (
    <div>
      <NotificationBar
        text='In the short run, you will be able to safely convert one private synthetic asset into the other synthetic asset'
      />
    </div>
  );
}
