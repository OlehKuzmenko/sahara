import styled from 'styled-components';

export const StyledActionContainer = styled.button`
  color: var(--theme-StyledColor-action);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: all 300ms ease-out;
`;
