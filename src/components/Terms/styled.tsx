import { StyledFAQContainer, StyledFAQTitle } from '@components/FAQ/styled';
import styled from 'styled-components';
import { StyledPrivacyContent } from '@components/PrivacyPolicy/styled';

export const StyledTermsContainer = styled(StyledFAQContainer)`
  max-width: 50vw;
`;

export const StyledTermsTitle = styled(StyledFAQTitle)`
`;

export const StyledTermsContent = styled(StyledPrivacyContent)`
`;